import React from "react";
import {
  Button,
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  TextInput,
  useColorScheme,
  View,
} from "react-native";

const Settings = () => {
  return (
    <SafeAreaView>
      <Text>THIS IS THE ADMIN SCREEN</Text>
    </SafeAreaView>
  );
};

export default Settings;
