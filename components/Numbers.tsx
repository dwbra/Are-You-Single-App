import React from "react";
import {
  Button,
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  TextInput,
  useColorScheme,
  View,
} from "react-native";

const Numbers = () => {
  return (
    <SafeAreaView>
      <Text>THIS IS THE NUMBERS SCREEN</Text>
    </SafeAreaView>
  );
};

export default Numbers;
